/node_modules
/out
/.pnp
.pnp.js
.DS_Store
*.log
.env*

!.env.example

# Generated by `prisma generate`, which runs from postinstall. Committing it
# pins a client build to one Prisma version, which then crashes any environment
# that installs a different one.
/src/generated
